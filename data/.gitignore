event_files07c68c6f-4439-4aed-af7a-38d067afb54c_202509150426_myce999.csv

# Client herd data must NOT be committed. These are raw exports and the files
# generated from them (regenerate locally by running step0_master_processing_my_data.R).
event_files/*.csv
intermediate_files/*.parquet
qc_files/*.parquet
template_files/template_event_details.*

# ...but keep the empty-directory placeholders and the non-sensitive templates
!.placeholder
!*/.placeholder

# 451-herd cross-herd extract used for ParnellBenchmark. The raw per-herd
# files carry real farm name/address/city/state (see build_parnell_benchmark.R),
# same reasoning as event_files/intermediate_files above — never commit them,
# and never commit the zipped bundle they were unpacked from.
parnell_files/silver/
silver.zip
# benchmark_monthly.rds is the anonymized, derived summary build_parnell_benchmark.R
# writes out of that data (no herd name/address/HerdKey — see its anonymity
# assertions) and is fine to commit like any other derived data/ file.
